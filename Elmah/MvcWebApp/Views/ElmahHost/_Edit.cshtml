@using Framework.Models;
@using Elmah.Models.Definitions;
@using Elmah.MvcWebApp.Models;
@using Elmah.Models;
@using Elmah.Resx;
@model MvcItemViewModel<ElmahHostModel>
@inject SelectListHelper selectListHelper;
@inject IUIStrings localizor;
@{
    var format_Please_Select = localizor.Get("Format_Please_Select");
}
@if (ViewBag.Status != System.Net.HttpStatusCode.OK)
{
    <div class="row">
        <div class="text-danger">@ViewBag.StatusMessage</div>
    </div>
}
else
{
    <input class="hidden-modal-title" value="@Model!.Model!.Host" type="hidden" />
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="Model!.Host" />
    <div class="form-group">
        <label asp-for="Model!.Host" class="control-label"></label>
        <input asp-for="Model!.Host" class="form-control" />
        <span asp-validation-for="Model!.Host" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Model!.SpatialLocation" class="control-label"></label>
        <input asp-for="Model!.SpatialLocation" class="form-control" />
        <span asp-validation-for="Model!.SpatialLocation" class="text-danger"></span>
    </div>
}
