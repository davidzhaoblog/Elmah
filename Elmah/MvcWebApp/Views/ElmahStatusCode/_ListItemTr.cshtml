@using Elmah.Resx;
@using Elmah.MvcWebApp.Models;
@using Framework.Models;
@using Elmah.Models;
@model MvcItemViewModel<ElmahStatusCodeModel>
@inject IUIStrings localizor;
@{
    var routeId = Model.Model?.StatusCode;
}
<tr class="pt-0 pb-0 nt-listitem@(Model.IsCurrentItem ? " nt-created border-warning border-3": "")" data-nt-route-id="@routeId">
    @if (Model.Template == ViewItemTemplateNames.Details.ToString())
    {
        @await Html.PartialAsync("~/Views/ElmahStatusCode/_ListDetailsItem.cshtml", Model)
    }
</tr>
