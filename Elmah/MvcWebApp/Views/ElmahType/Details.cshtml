@using Elmah.Models;
@using Elmah.Resx;
@model ElmahTypeModel
@inject IUIStrings localizor;
@{
    ViewData["Title"] = localizor.Get("Details");
}
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h1>@localizor.Get("Details")</h1>
                <h4>@localizor.Get("ElmahType")</h4>
            </div>
            @if (ViewBag.Status != System.Net.HttpStatusCode.OK)
            {
                <div class="card-body">
                    <div class="text-danger">@ViewBag.StatusMessage</div>
                </div>
            }
            else
            {
                <div class="card-body">
                    @if (ViewBag.Status != System.Net.HttpStatusCode.OK)
                    {
                        <div class="text-danger">@ViewBag.StatusMessage</div>
                    }
                    <dl class="row">
                        <dt class="col-sm-2">
                            @Html.DisplayNameFor(model => model.Type)
                        </dt>
                        <dd class="col-sm-10">
                            @Html.DisplayFor(model => model.Type)
                        </dd>
                    </dl>
                </div>
            }
            <div class="card-footer">
                <div class="btn-group" role="group" aria-label="...">
                    <a asp-area="" asp-controller="ElmahType" asp-action="Details" asp-route-type="@Model?.Type" class="btn btn-secondary" role="button" title="@localizor.Get("Refresh")">
                        <i class="fa-solid fa-refresh"></i>
                        <span class="submit-text hidden-xs hidden-sm visible-md-inline visible-lg-inline">@localizor.Get("Refresh")</span>
                    </a>
                    <a asp-area="" asp-controller="ElmahType" asp-action="Edit" asp-route-type="@Model?.Type" class="btn btn-secondary" role="button" title="@localizor.Get("Delete")">
                        <i class="fa-solid fa-pen-to-square"></i>
                        <span class="submit-text hidden-xs hidden-sm visible-md-inline visible-lg-inline">@localizor.Get("Edit")</span>
                    </a>
                    <a asp-area="" asp-controller="ElmahType" asp-action="Delete" asp-route-type="@Model?.Type" class="btn btn-secondary" role="button" title="@localizor.Get("Delete")">
                        <i class="fa-solid fa-trash"></i>
                        <span class="submit-text hidden-xs hidden-sm visible-md-inline visible-lg-inline">@localizor.Get("Delete")</span>
                    </a>
                    <a asp-area="" asp-controller="ElmahType" asp-action="Index" class="btn btn-secondary" role="button" title="@localizor.Get("Back_to_List")">
                        <i class="fa-solid fa-arrow-left"></i>
                        <span class="submit-text hidden-xs hidden-sm visible-md-inline visible-lg-inline">@localizor.Get("Back_to_List")</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
