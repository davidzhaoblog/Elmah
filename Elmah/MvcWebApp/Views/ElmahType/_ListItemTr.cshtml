@using Elmah.Resx;
@using Elmah.MvcWebApp.Models;
@using Framework.Models;
@using Elmah.Models;
@model MvcItemViewModel<ElmahTypeModel>
@inject IUIStrings localizor;
@{
    var routeId = Model.Model?.Type;
}
<tr class="pt-0 pb-0 nt-listitem@(Model.IsCurrentItem ? " nt-created border-warning border-3": "")" data-nt-route-id="@routeId">
    @if (Model.Template == ViewItemTemplateNames.Details.ToString())
    {
        @await Html.PartialAsync("~/Views/ElmahType/_ListDetailsItem.cshtml", Model)
    }
    else if (Model.Template == ViewItemTemplateNames.Edit.ToString())
    {
        @await Html.PartialAsync("~/Views/ElmahType/_ListEditItem.cshtml", Model)
    }
    else if (Model.Template == ViewItemTemplateNames.Create.ToString())
    {
        @await Html.PartialAsync("~/Views/ElmahType/_ListCreateItem.cshtml", Model)
    }
</tr>
